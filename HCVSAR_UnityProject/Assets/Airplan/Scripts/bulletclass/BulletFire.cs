using UnityEngine;
using System.Collections;

public struct BulletFire
{
    //public int ppp = 1;
   /*
    public BulletFire(int B_loop_Qt, int B_ring_quantity,)
    {
        float Tt =0;
        float qr =0;
        Quaternion qq =;
        if (B_loop_Qt > 0 && Time.time > Tt)
        {


            for (int i = 0; i < B_ring_quantity; i++)
            {

                Fire(transform.position, qq);
                qq.eulerAngles = new Vector3(qq.eulerAngles.x, qq.eulerAngles.y + qr, qq.eulerAngles.z);

            }
            Tt = Time.time + B_CD;
            B_loop_Qt--;
        }
    }

    public void Fire(Vector3 pos, Quaternion rota)
    {

        GameObject bullet = Instantiate(B_bullet, pos, rota) as GameObject;
        bullet.transform.position += (bullet.transform.forward * B_appearD);
    }*/

    public void jjj()
    {

    }

}
